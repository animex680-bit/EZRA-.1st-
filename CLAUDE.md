# Claude Code — Project Instructions

## Workflow: Landing Page Delivery

After completing any landing page feature or update, always finish with these steps in order:

1. Commit and push all changes to GitHub (`git push -u origin main`)
2. Confirm GitHub Pages is enabled on the repo (enable via API if not already)
3. Provide the user with the live links:
   - **GitHub Pages:** `https://animex680-bit.github.io/EZRA-.1st-/`
   - **Vercel** (if deployed): the `vercel.app` URL from the project dashboard

Never close out a landing page task without giving the user a clickable live URL they can send to prospects.

## Credentials (do not share publicly)

- GitHub username: `animex680-bit`
- GitHub repo: `EZRA-.1st-`
- GitHub Pages URL: `https://animex680-bit.github.io/EZRA-.1st-/`

## Project Context

This is the **Tala Med Spa** landing page project. The main file is `index.html`. The `project/` folder contains design prototypes — use `index.html` as the production file.
